# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Use Android Studio bundled JDK (21) instead of system JDK (25)
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr

# AndroidX
android.useAndroidX=true

# Kotlin
kotlin.code.style=official

# Non-transitive R classes
android.nonTransitiveRClass=true

# Build config
android.defaults.buildfeatures.buildconfig=true

# Google Maps API Key (replace with real key)
MAPS_API_KEY=placeholder-replace-with-real-key

# API Configuration (override per environment)
# STAGING desativado temporariamente (planejado para retorno em 2-3 meses).
# API_BASE_URL_STAGING="https://pyloto-atende-820238173887.us-central1.run.app/"
API_BASE_URL_PRODUCTION="https://pyloto-atende-820238173887.us-central1.run.app/"
